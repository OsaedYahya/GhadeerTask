import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
  containerStyle: {
    elevation: 2,
    padding: 8,
    backgroundColor: "#fff",
    marginVertical: 6,
    borderRadius: 10
  },
  titleStyle: {
    fontSize: 18
  }
});

export default styles
